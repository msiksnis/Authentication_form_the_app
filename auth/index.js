// Import a library to help creat a component
import { AppRegistry } from "react-native";
import App from "./src/App";

// Create a component

// Render it to he device
AppRegistry.registerComponent("auth", () => App);
